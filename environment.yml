name: ssi
channels:
  - conda-forge
dependencies:
  - python=3.11
  - numpy
  - scipy
  - scikit-learn
  - pandas
  - pyserial
  - matplotlib
  - jupyter
  - pip
  - pip:
      - pyqtgraph
      - neurokit2
      # Dev tools — ruff handles both lint and format; mypy strict; tests
      - ruff
      - mypy
      - pre-commit
      - pytest
      - pytest-cov
      - nbstripout
